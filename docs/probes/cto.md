# CTO worktree probe

The CTO agent worktree at `/opt/cinetrace-ai/worktrees/cto` can read and push to the private `asaf-avron/cinetrace-ai` repository.

This file is a bootstrap sanity check — not product documentation.
