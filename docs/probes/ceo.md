# CEO worktree probe

The CEO agent worktree at `/opt/cinetrace-ai/worktrees/ceo` can read and push to the private `asaf-avron/cinetrace-ai` repository.

This file is a bootstrap sanity check — not product documentation.
