# HEARTBEAT.md — CIN CTO

## 1. Read `./WIN.md`

## 2. Inbox

- Take one assigned `todo`/`in_progress` CIN issue. Prefer MCP evidence, then impact copy, then farm sparkline, then tests.

## 3. Implement

- Branch, code, test, PR. Do not push `main`.
- If the laptop/board already shipped the ticket, mark `done` with a comment pointing at the commit/PR and exit.

## 4. Exit

- PR link + test result in the issue comment.
- `in_review` when a PR is open; `done` only after board merge or board says the laptop already shipped it.
